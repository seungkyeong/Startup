<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nakwon.mapper.ReservationHoldMapper">
	<!-- 예약 등록 -->
	<insert id="insertReservationHold">
		insert into ReservationHold (RsrvCode, Phone, Name, Email, RsrvDate, Pnum, Message, Code, MenuCode)
		values (#{RsrvCode}, #{Phone}, #{Name}, #{Email}, #{RsrvDate}, #{Pnum}, #{Message}, #{Code}, #{MenuCode})
	</insert>
	
	<select id="rsrvHoldListAll" resultType="com.nakwon.domain.ReservationVO">
	<![CDATA[
	select *
	from ReservationHold
	]]>
	</select>
	
</mapper>